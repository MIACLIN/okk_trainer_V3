{
  "permissions": {
    "allow": [
      "PowerShell(if \\(-not $?\\) { \"Directory is empty or doesn't exist - creating structure\" })",
      "Bash(npm create *)",
      "Read(//usr/local/**)",
      "Read(//usr/**)",
      "Bash(command -v node)",
      "Bash(command -v npm)"
    ]
  }
}
