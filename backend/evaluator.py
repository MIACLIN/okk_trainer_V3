import json
import os
import re
from dotenv import load_dotenv
from openai import OpenAI
from scenarios import Scenario

load_dotenv()

client = OpenAI(
    api_key=os.environ.get("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com",
)

DEEPSEEK_MODEL = "deepseek-chat"

SYSTEM_PROMPT = """Ты — эксперт по качеству продаж в медицинских клиниках.
Твоя задача: оценить работу медицинского куратора на первичной консультации
с пациентом стоматологической клиники.
Куратор — не врач. Его задача: выявить запрос, снять страхи, установить контакт,
собрать анамнез (ситуацию), выявить ЛПР, отработать возражения и закрыть на запись.

ЧЕКЛИСТ ОЦЕНКИ (10 критериев, каждый от 0 до 10):

1. Знакомство и установление контакта (0–10)
• Представился ли куратор по имени и роли?
• Объяснил ли структуру визита?
• Был ли тон тёплым, без формализма?

2. Выявление запроса (0–10)
• Спросил ли с чем пациент пришёл — открытым вопросом?
• Уточнил ли детали: какой зуб, как давно, что беспокоит?
• Не перешёл ли сразу к решению, не дослушав пациента?

3. Сбор истории (0–10)
• Выяснил ли, как давно проблема, что уже делалось, где лечился раньше?
• Спросил ли про предыдущий опыт в стоматологии?
• Естественно ли встраивал вопросы в диалог?

4. Выявление страхов и барьеров (0–10)
• Спросил ли про страх стоматолога / боли / операции?
• Не проигнорировал ли сигналы тревоги?
• Дал ли пациенту высказаться до конца?

5. Работа со страхами (0–10)
• Нормализовал ли страх («это понятно, многие так же»)?
• Объяснил ли, что будет на приёме сегодня (только осмотр, без боли)?
• Не отмахнулся ли от страха дежурной фразой?

6. Выявление ЛПР (0–10)
• Спросил ли, кто ещё участвует в решении?
• Если есть ЛПР — предложил ли позвонить или прийти вместе?
• Не давил ли принять решение единолично?

7. Мотивация к лечению (0–10)
• Выяснил ли, почему пациент решил прийти именно сейчас?
• Нашёл ли личный мотив?
• Использовал ли этот мотив в разговоре?

8. Финансовый блок (0–10)
• Назвал ли примерную стоимость уверенно, без извинений?
• Правильно ли обосновал цену (ценность, не скидка)?
• Упомянул ли рассрочку уместно?

9. Закрытие на запись (0–10)
• Предложил ли конкретную дату и время?
• Не отпустил ли пациента без договорённостей?
• При отказе — предложил ли альтернативу?

10. Эмпатия и адаптация (0–10)
• Было ли ощущение искреннего интереса к пациенту?
• Не скатился ли куратор в формальный скрипт?
• Адаптировал ли стиль под конкретного пациента?

ВАЖНО:
• Цитируй конкретные реплики из диалога — не обобщай без примера.
• Не снижай балл за то, что пациент был сложным — оценивай реакцию куратора.
• Ошибки формулируй конструктивно: не «плохо сделал», а «вместо X лучше Y».
• Конкретная альтернативная фраза обязательна в каждой ошибке.
• Не выставляй завышенные оценки из вежливости.

Верни ТОЛЬКО валидный JSON (без дополнительного текста):
{
  "total_score": <сумма всех 10 оценок>,
  "level": "<Новичок|Развивается|Уверенный|Профи>",
  "criteria": [
    {"name": "...", "score": <0-10>, "comment": "одно предложение"},
    ...10 элементов
  ],
  "top_errors": [
    {"title": "...", "what": "цитата или момент из диалога", "why": "почему это важно", "how": "альтернативная фраза"},
    ...3 элемента
  ],
  "strengths": "1–3 конкретных сильных момента с примером из диалога",
  "priority": "Одно конкретное умение для следующей сессии"
}

Уровни: Новичок 0–39, Развивается 40–59, Уверенный 60–79, Профи 80–100."""


def evaluate_session(scenario: Scenario, history: list[dict]) -> dict:
    patient_name = scenario["patient"]["name"]

    transcript_lines = []
    for msg in history:
        role = "[К]" if msg["role"] == "user" else f"[П] ({patient_name})"
        transcript_lines.append(f"{role}: {msg['content']}")
    transcript = "\n".join(transcript_lines)

    # Determine typology label from scenario title
    typology_map = {
        "typology_a_scared":   "Боится",
        "typology_b_bargains": "Торгуется",
        "typology_c_silent":   "Молчун",
    }
    typology = typology_map.get(scenario["id"], "Неизвестный")

    user_prompt = (
        f"Вот транскрипт консультации. Куратор — строки [К]:, пациент — строки [П]:. "
        f"Типаж пациента: {typology}.\n\n"
        f"Оцени работу куратора по чеклисту.\n\n"
        f"[ТРАНСКРИПТ]\n{transcript}\n[/ТРАНСКРИПТ]"
    )

    response = client.chat.completions.create(
        model=DEEPSEEK_MODEL,
        max_tokens=2048,
        temperature=0.3,
        messages=[
            {"role": "system", "content": SYSTEM_PROMPT},
            {"role": "user", "content": user_prompt},
        ],
    )

    raw = (response.choices[0].message.content or "").strip()
    json_match = re.search(r'\{[\s\S]*\}', raw)
    if json_match:
        raw = json_match.group(0)

    data = json.loads(raw)

    # Normalise / fill gaps
    criteria = data.get("criteria", [])
    criterion_names = [
        "Знакомство и установление контакта",
        "Выявление запроса", "Сбор истории",
        "Выявление страхов и барьеров", "Работа со страхами",
        "Выявление ЛПР", "Мотивация к лечению",
        "Финансовый блок", "Закрытие на запись", "Эмпатия и адаптация",
    ]
    while len(criteria) < 10:
        i = len(criteria)
        criteria.append({"name": criterion_names[i], "score": 0, "comment": "Не удалось оценить."})

    total = sum(c.get("score", 0) for c in criteria)

    return {
        "total_score": total,
        "max_score": 100,
        "level": data.get("level", _level(total)),
        "criteria": criteria,
        "top_errors": data.get("top_errors", []),
        "strengths": data.get("strengths", ""),
        "priority": data.get("priority", ""),
    }


def _level(score: int) -> str:
    if score >= 80: return "Профи"
    if score >= 60: return "Уверенный"
    if score >= 40: return "Развивается"
    return "Новичок"
